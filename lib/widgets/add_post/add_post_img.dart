import 'package:flutter/material.dart';

class AddPostImg extends StatelessWidget {
  const AddPostImg({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 80,
      width: 80,
      color: Colors.blue,
      child: Icon(Icons.image),
    );
  }
}
